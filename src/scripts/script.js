/* global tippy */

// let tippyInstances = [];
// const singleton = tippy.createSingleton([], {
//   placement: 'left',
//   allowHTML: true,
//   maxWidth: 500,
//   delay: 100,
// });

tippy('[data-tippy-content]', {
  placement: 'right',
  allowHTML: true,
  maxWidth: 200,
  theme: 'cifrodent',
});
